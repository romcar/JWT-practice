import { combineReducers } from 'redux';

export default rootReducer = combineReducers({
// place all reducers here mimicing the shape
// of your initial state.
//    key: keyReducer

// don't forget to import the reducers
});